To run the frontend you should:
open cmd and put those commands =>
1)cd <absolute path of frontend folder>
2)npm run dev
3)press o + enter

To run the backend you should:
open another cmd terminal and put those commands =>
1)cd <absolute path of backend folder>
2)npm run dev

(any new email/passward<hashed> will be stocked in my mongodb cloud)